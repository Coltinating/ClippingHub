PORT=3535
DATA_DIR=./data
LOG_LEVEL=info
MAX_LOBBIES=200
LOBBY_TTL_HOURS=24
# Required for admin sessions. If unset the server prints a random dev token
# on boot — fine for local development, NOT for any deployment.
ADMIN_TOKEN=test-token-123
